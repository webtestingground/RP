export interface Persona {
  id: string;
  name: string;
  emoji: string;
  tagline: string;
  description: string;
  systemPrompt: string;
  greeting: string;
  placeholderText: string;
  color: string;
}
